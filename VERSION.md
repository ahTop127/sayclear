# SayClear v0.1.0

- **版本号：** `v0.1.0`
- **日期：** 2026-09-03
- **状态：** 可跑通的第一版 Mac 客户端（MVP）
- **标签含义：** 主路径已经能用，还不是加速版

## 这一版已经有什么

- Mac 菜单栏应用（图标 `SC`），全局 ⌘ 单击切换开始/结束录音
- 底部暗黑胶囊：左 X 取消，中间波形，右 OK 结束；结束后只显示 Thinking
- 语音识别：Google Cloud Speech-to-Text V2 `chirp_3`（说完后整段 REST 识别）
- 文本整理：DeepSeek V4 Flash，一次调用完成清洗 / 改口 / 分点 / 格式化 / 锁定意图
- 成功后正文直接填入当前输入框；不代发、无成功 toast

## 这一版明确还不是什么

- **不是加速版。** 识别和整理都在用户点 OK 之后串行发生，等待偏长。
- **不是边说边出字。** 录音中不把半句填进输入框（产品如此，不是没做完）。
- 加速技术方案（说话期间后台识别、换更快整理入口）已评审，**未写入本版代码**。

## 运行

密钥只放本机 `.env`（已 gitignore）。需要 Anaconda Python 3.13、麦克风权限、辅助功能权限。

```bash
./run_sayclear.sh
```
